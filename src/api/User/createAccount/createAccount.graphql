type CreateAccountResponse {
  ok: Boolean!
  error: String
}

type Mutation {
  createAccount(
    username: String!
    email: String!
    bio: String
  ): CreateAccountResponse!
}

type Query {
  something: String!
}
